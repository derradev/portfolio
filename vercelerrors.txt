✅ FIXED: Certifications & Education Issues

❌ Issues Found:
1. Certifications route using deprecated query() at line 10
2. Education table missing 'location' column (42703 error)

✅ FIXES APPLIED:

**1. Certifications Route:**
- OLD: `query('SELECT id, name, issuer... FROM certifications...')`
- NEW: `getClient().from('certifications').select().order()`
- Converted to proper Supabase client method

**2. Education Location Column:**
- ADDED: `location VARCHAR(255)` column to education table in SQL script
- KEPT: All location references in education route code
- SOLUTION: Add missing column instead of removing functionality

**3. Updated SQL Fix Script:**
- Added `ALTER TABLE education ADD COLUMN IF NOT EXISTS location VARCHAR(255)`
- Will add location column when run in Supabase

🔧 NEXT: Run updated fix-missing-columns.sql to add location column
🚀 RESULT: Both certifications and education APIs should work properly!