✅ FIXED: Blog Routes Raw SQL Queries

❌ Previous Error: "Raw SQL queries deprecated" in blog.js:109:45

✅ FIXES APPLIED:
- GET /:slug → Converted queryOne() to getClient().from().select().eq().single()
- GET /admin/all → Converted query() to getClient().from().select().order()
- Removed references to missing columns (author, read_time)
- All blog routes now use proper Supabase client methods

🚀 Blog API should now work without deprecated method errors!